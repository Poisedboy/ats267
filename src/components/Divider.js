// import React, { useEffect, useState } from "react";
// import { Image } from "react-bootstrap";
// import geometryPic from '../assets/geometry1.jpg';

const Divider = ({ dividerPicture }) => {
    // const [mobileOpen, setMobileOpen] = useState(false);

    // useEffect(() => {
    //     if (window.innerWidth < 500) {
    //         setMobileOpen(prevState => !prevState);
    //     };
    // }, []);

    return ( <div></div>
        // <div>
        //     <Image src={geometryPic} alt='Divider Picture' style={mobileOpen ? { width: '100%', height: '70px', objectFit: 'cover'} : { width: '100%', height: '110px', objectFit: 'cover'}} />
        // </div>
    )
};

export default Divider;